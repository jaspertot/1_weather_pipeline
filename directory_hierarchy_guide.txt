weather-pipeline/
│
├── .env                          # API keys and GCP credentials (never commit this)
├── .gitignore                    # Must include .env and credentials/
├── requirements.txt              # All Python dependencies with pinned versions
├── README.md                     # Project overview and setup instructions
│
├── ingestion/
│   ├── __init__.py
│   ├── fetch_weather.py          # Calls OpenWeatherMap API per city
│   └── cities.py                 # Your list of 10-15 target cities
│
├── raw_data/
│   └── .gitkeep                  # Keeps the folder in Git even when empty
│
├── transformation/
│   ├── __init__.py
│   ├── clean.py                  # Flattening, type casting, null handling
│   ├── enrich.py                 # Derived columns: heat index, parsed datetime
|   └── run_transformation.py     # calls clean then enrich, saves final CSV
│   └── validate.py               # Pre-load sanity checks (ranges, nulls, dupes)
│
├── loading/
│   ├── __init__.py
│   └── bigquery_loader.py        # Loads cleaned DataFrame into BigQuery
│
├── quality/
│   ├── __init__.py
│   ├── expectations/             # Great Expectations config and expectation suites
│   └── run_checks.py             # Entry point to run all quality checks
│
├── orchestration/
│   ├── __init__.py
│   └── dags/
│       └── weather_dag.py        # Airflow DAG defining the full pipeline schedule
│
├── dashboard/
│   ├── __init__.py
│   ├── app.py                    # Main Streamlit entry point
│   └── queries.py                # All BigQuery SQL queries used by the dashboard
│
├── credentials/
│   └── .gitkeep                  # Where your GCP service account JSON key lives
│                                 # The actual key file must be in .gitignore
│
├── logs/
│   └── .gitkeep                  # Pipeline run logs land here
│
└── tests/
    ├── __init__.py
    ├── test_ingestion.py         # Unit tests for the fetch functions
    ├── test_transformation.py    # Unit tests for clean and enrich logic
    └── test_loading.py           # Tests for the BigQuery loader